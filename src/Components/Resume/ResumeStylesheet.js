import { makeStyles, createStyles } from '@material-ui/core';

const ResumeStylesheet = makeStyles(theme => createStyles({
  headerStyles: {
    color: theme.palette.red
  }
}));

export default ResumeStylesheet;