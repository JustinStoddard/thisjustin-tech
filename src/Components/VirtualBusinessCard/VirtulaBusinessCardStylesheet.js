import { makeStyles, createStyles } from '@material-ui/core';

const VirtualBusinessCardStylesheet = makeStyles(theme => createStyles({
  headerStyles: {
    color: theme.palette.red
  }
}));

export default VirtualBusinessCardStylesheet;