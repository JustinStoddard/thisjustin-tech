import { makeStyles, createStyles } from '@material-ui/core';

const SpacexStylesheet = makeStyles(theme => createStyles({
  headerStyles: {
    color: theme.palette.red
  }
}));

export default SpacexStylesheet;